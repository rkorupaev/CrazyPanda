import styles from "../styles/Table.module.scss";

const Column = (props) => {
    return (
        <>
            <th className={styles.table_column}></th>
        </>
    )
}

export default Column;
